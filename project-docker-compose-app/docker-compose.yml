version: '3.8'
services:
 web:
  build: .
  ports:
   - "8000:5000"
  volumes:
   - .:/code
  environment:
   - FLASK_DEBUG=1
 redis:
  image: "redis:alpine"
  volumes:
   - tidi:/data
volumes:
 tidi:
